# foundation.cbsa_features

- Grain: one row per `cbsa_code`, `year`
- Published by: `foundation_feature_workflow.R`
- Build asset: `build.sql`
- Status: migrated to table-owned layer asset

This folder is the canonical home for the SQL that builds `foundation.cbsa_features` before publication to DuckDB.
